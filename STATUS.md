# Jotter — Project Status

- **Current Version:** v0.9.0 (v0 spec contracts complete; v1 work in progress)
- **Last Updated:** 2026-08-05 (Live surface selection formatting toolbar — Bold/Italic/Strike/Code)
- **Repo:** https://github.com/suporterfid/jotter
- **Production Site:** https://hub.taskconnect.com.br/
- **CI Status:** 🟢 Green on `main`. #140 and #142 fixed via PR #144, merged and confirmed on two green GitHub Actions runs, both issues closed. **`main` is now branch-protected** (#148): the `test` CI job is a required status check, `enforce_admins` is on, force-pushes and deletions are disabled — direct pushes and merges without green CI are rejected by GitHub. Verified live: an empty direct-push commit was rejected with `Required status check "test" is expected`. This is the mechanism that was missing when #140 regressed silently after #49.
- **Planning authority:** `docs/jotter-initial-spec-and-build-plan.md` §14 sequences post-v0 work from `docs/20260727-jotter-roadmap-ai-agent.md`

---

## 1. Accomplished (v0 spec and beyond)

- **PR0 — Scaffold & CI**: Laravel 12 + Vue 3 SPA + Vite + Docker dev + `jt` scripts + release target.
- **PR1 — Data Model & Migrations**: Idempotent schema (`tenants`, `workspaces`, `notes`, `note_links`, `tags`, `note_tags`, `attachments`, `users`, `memberships`, `audit_log`).
- **PR2 — Vault Storage Service**: Plain `.md` files on disk, front-matter parsing, `VaultPathGuard` traversal protection, `vault:reindex` Artisan command.
- **PR3 — Links & Backlinks**: `[[wikilinks]]` projected into `note_links`, resolved and unresolved refs retained.
- **PR4 — Full-Text Search**: MySQL `FULLTEXT` over title and content (`GET /api/workspaces/{id}/search`).
- **PR5 — Workspace Notes CRUD API**: Workspace-scoped notes endpoints.
- **PR6 — Frontend Vue 3 SPA**: Glassmorphism UI, Markdown editor, `[[` autocomplete, backlinks panel.
- **PR7 — Auth Abstraction**: `IdentityProvider` seam with `LocalIdentityProvider` and a `GrandpaSSOnIdentityProvider` adapter; `AuthorizeWorkspaceAccess` replaced the fail-closed placeholder.
- **PR8 — Attachment Management**: Uploads to vault `_resources/` with a 20 MB type/size allowlist and streaming endpoints.
- **PR9 — Deployment Hardening**: Shared-hosting deployment, AutoSSL, production `.env` configuration.
- **Post-v0 enhancements**: command palette, drag & drop uploads, GFM task lists, code-block copy, tag cloud + sorting, note graph view, sync endpoint, ZIP export, audit-log query,  - Server-Side CommonMark Renderer & XSS Sanitizer (`MarkdownServerRenderer.php`)
  - Background Job Dispatcher Seam (`JobDispatcher` + `LocalJobDispatcher`)
  - WebDAV Adapter (`WebDavController`) with `PROPFIND`, `GET`, `PUT`, `MKCOL`, `DELETE`, `OPTIONS`
  - Note Rename & Relocation Endpoint (`POST /api/workspaces/{w}/notes/{n}/move`)
  - Case-Insensitive Wikilink Resolution Policy (`WikilinkProjector.php`)
  - Audit Log Hardening Suite (`AuditEvent` enum, `AuditRecorder` with automatic redaction, append-only immutability, tenant-scoped queries)
  - Audit Log Retention & Pruning Command (`audit:prune --days=90` with chunked deletion)
  - Hardened Vault Import Pipeline (`VaultExtractor` with Zip-Slip protection, `POST /api/workspaces/{w}/import` endpoint, overwrite collision policy, and full export-import round-trip equivalence)
  - Typed Property Model Specification & Inference Matrix (`NotePropertyType` enum & decision record in `docs/decisions.md`)
  - Rebuildable Typed Property Projection (`note_properties` table, `NotePropertyProjector`, and drop-and-rebuild parity test `NotePropertyProjectionTest.php`)
  - Typed Properties API & Front-Matter Write-Through (`GET /api/workspaces/{w}/properties`, `POST` & `DELETE` note properties with disk write-through)
  - Admin Workspace CRUD & Validated Vault Root (`POST /api/admin/workspaces`, `PUT`, `archive`, `VaultRootGuard` base path confinement & nesting collision protection)
  - Admin Membership & Role Management (`GET/POST/PUT/DELETE /api/admin/workspaces/{w}/members`, role definitions, last owner protection)
  - Admin Local User Management (`GET/POST /api/admin/users`, deactivate, reactivate, password reset, self-service change password, provider gating)
  - Admin Administration UI (`frontend/src/components/AdminPanel.vue`, Workspaces/Members/Users tabs, modal & Vitest test suite)
  - Declarative Block Registry (`app/Domain/Vault/BlockRegistry.php`, `frontend/src/services/blockRegistry.ts`, PHPUnit `BlockRegistryTest` & Vitest `blockRegistry.spec.ts`)
  - Callouts, Toggles, Tables, and Dividers Dual-Path Rendering (`MarkdownServerRenderer.php` with TableExtension & callout regex, `markdown.ts` callout renderer)
  - Slash-Command Insertion Menu (`SlashMenu.vue` component driven by `blockRegistry.ts`, filter/navigation, `SlashMenu.spec.ts`; wired into `NoteEditor.vue`'s `handleInput`/`handleKeyDown` via #256 — see §3, previously built but never mounted anywhere)
  - MCP HTTP Transport & Machine-Token Auth (`POST /api/mcp`, `machine_tokens` table, SHA-256 tokens via `IdentityProvider` seam, `McpTransportAuthTest`)
  - Read-Only MCP Tools & Cross-Workspace Denial (`list_notes`, `read_note`, `search_notes`, `get_backlinks`, `McpReadOnlyToolsTest`)
  - MCP Write Tools Decision & Scope (`docs/mcp.md`, write tools intentionally deferred and gated per §8 S2 & S5)
  - Visual Identity Specification & Brand Assets (`docs/visual-identity.md`, `assets/brand/README.md`, brand assets inventory)
  - Visual Identity Semantic Design-Token Layer (`frontend/src/styles/tokens.css` with semantic variables, status extension tokens & contrast ratios table in `docs/visual-identity.md`)
  - Visual Identity Self-Host Open Sans & Remove Google Fonts CDN (`frontend/src/styles/fonts.css`, WOFF2 files under `frontend/src/assets/fonts/`, removed Google Fonts CDN links)
  - Visual Identity: Migrate SPA components off raw color literals onto semantic tokens (#100 — all 8 components, glassmorphism retired, `--color-*` tokens throughout)
  - Visual Identity: Type scale, responsive headings, heading hierarchy (#101 — scale tokens added to `tokens.css`, `style.css` H1 fixed from 6.5rem/lh-0.9 to fluid clamp, ≥16px prose enforced)
  - Visual Identity: Spacing, layout widths, radius, borders, elevation — retire glassmorphism (#102 — `--space-*`/`--radius-*` tokens everywhere, all `backdrop-filter` removed, solid overlay backgrounds, radial-gradient body removed)
  - Visual Identity: Buttons, links, inputs, focus rings, touch targets (#103 — all six `outline: none` declarations removed, global `:focus-visible` ring added in `App.vue`, `min-height: 36–44px` touch targets enforced, primary/secondary button vocabulary defined)
  - Visual Identity: Motion tokens and prefers-reduced-motion support (#104 — `--duration-*`/`--ease-standard` used across SPA, `@media (prefers-reduced-motion: reduce)` block added to `tokens.css`)
  - Visual Identity: Reconcile landing and welcome-page themes (#105 — removed unused `welcome.blade.php` and `tailwindcss` devDependencies, cleaned `style.css` base layer, documented import order in `main.ts`)
  - Visual Identity: Theme published static site (#106 — Blade view `publish/page.blade.php` + `publish.css` + self-hosted Open Sans WOFF2 fonts copied to output, responsive reading column, visited link styles, reduced motion block)
  - Visual Identity: App-shell document metadata — favicon, theme-color, color-scheme, social card (#107 — `app.blade.php` and `index.html` synchronized with `color-scheme: dark`, `theme-color: #000000`, favicons SVG/ICO/Apple, OG & Twitter cards)
  - Visual Identity: Project mark, wordmark, favicon, and social card (#108 — `assets/brand/` directory structured with brand guidelines `assets/brand/README.md`, wordmark added to `README.md`)
  - Visual Identity: WCAG 2.2 AA audit and axe-core regression test (#109 — `frontend/src/a11y.spec.ts` automated axe-core Vitest spec covering all SPA views, documented contrast audit matrix and manual checklist in `docs/visual-identity.md`)
  - Visual Identity: CI guard against raw color literals and unapproved font sources (#110 — `./scripts/check-design-tokens.sh` executable guard script enforcing raw color literal ban, palette token ban, font CDN ban, and un-annotated outline:none ban)
  - Visual Identity: Notion-inspired redesign — light/dark theming, neutral accent, Inter typeface, minimal elevation, mobile layout (2026-07-30 — `docs/superpowers/specs/2026-07-30-notion-visual-identity-design.md`, `scripts/check-design-tokens.sh` extended with theme-parity check)
  - Milestone A: Filtered search by title, tags, and modified-date range (#52 — `SearchCriteria` value object, extended `GET /api/workspaces/{w}/search` params, index-backed multi-tag/title/date filtering)
  - CI stability: Playwright notes.spec.ts login wait & SPA error surfacing (#49 — aligned login wait to 10s, surfaced createNote errors in SPA)
  - Milestone A: Hardened archive extraction (#76 — `VaultExtractor` domain service with zip-slip, symlink, path-aliasing, type allowlist, and size/entry bounds)
  - Milestone A: Bounded import job and upload endpoint (#77 — `VaultImportCommand` Artisan command, `POST /api/workspaces/{w}/import` endpoint, staged upload cleanup)
  - Milestone A: Collision policy and JSON backup format round-trip (#78 — `VaultBackupRoundTripTest` equivalence suite, JSON v1.0 backup export/import support, configurable overwrite collision policy)
  - Milestone B: Typed property model design decisions (#79 — recorded in `docs/decisions.md`)
  - Milestone B: Rebuildable typed property projection (#80 — `note_properties` schema, `NotePropertyProjector`, `VaultReindexer` integration, drop-and-rebuild test)
  - Milestone B: Expose properties in notes API (#81 — `GET /api/workspaces/{w}/properties`, front-matter write-through via `VaultStorage`)
  - Milestone B: Broken-link and orphan report (#55 — `GET /api/workspaces/{w}/link-report` endpoint returning unresolved wikilinks and orphan notes)
  - Milestone A: Note version history with restore (#51 — `note_revisions` schema, `NoteRevisions` service with content-hash deduplication, `vault:prune-revisions` command, REST revision endpoints)
  - Milestone B: Daily notes and note templates (#56 — `_templates/` folder, `TemplateEngine` variable substitution, `POST /notes/from-template`, `GET|POST /daily/{date?}`)
  - Milestone B: Richer block and slash-command surface epic (#57 — declarative block registry, dual-path sanitization, callouts, toggles, tables, dividers, slash insertion menu)
  - Milestone B: MCP server epic (#58 — machine-token auth over IdentityProvider seam, read-only vault tools & resources)
  - Milestone C: GrandpaSSOn identity adapter (#59 — `GrandpaSSOnIdentityProvider` adapter over `IdentityProvider` seam)
  - Milestone C: Workspace and membership administration epic (#60 — workspace CRUD, membership & role management, local user management)
  - Spec Debt: Audit log hardening epic (#64 — standardized event vocabulary, redaction, append-only DB enforcement, retention)
  - Spec Debt: Note identity epic (#65 — path rename/move endpoint, wikilink case-insensitive & ambiguity resolution policy)
  - Visual Identity: Shared dark/purple design system epic (#96 — semantic token layer, Open Sans typography, WCAG 2.2 AA audit, CI token guard)
  - Decisions: Resolved all six roadmap/spec conflicts C1–C6 in `docs/decisions.md` (#50)
  - Tracking: Completed post-v0 implementation plan tracking (#68)
  - Milestone C: Inline comments and mentions (#61 — `note_comments` schema, `WorkspaceCommentController`, mention parsing)
  - Milestone C: Notification and event bus (#62 — `notifications` schema, `WorkspaceEventEmitter`, `WorkspaceNotificationController`)
  - Milestone D: Structured collections and views (#63 — `WorkspaceCollectionController` table view over `note_properties`)
  - v2 Epic: Heavy content processing delegated to TaskConnect (#67 — `docs/taskconnect-integration.md` delegation contract)

---

## 2. Position against the product roadmap

`docs/20260727-jotter-roadmap-ai-agent.md` ranks twelve near-term priorities. **Five of its top six already ship here** — search, nested folders, tags, backlinks, and attachments. Its gap analysis says otherwise because its baseline describes a different product (offline-first, Material You, realtime collaboration); spec §14.1 records this and §14.3 carries the authoritative delivered-state table.

| Roadmap milestone | State |
|---|---|
| A — knowledge foundations | Mostly delivered. Open: version history, search filters, import. |
| B — connected knowledge | Partial. Command palette and graph view ship; templates, broken-link report, typed properties, MCP server open. |
| C — team collaboration | Data model and per-request authorization ship. GrandpaSSOn adapter, admin UI, comments, notifications open. |
| D — structured work | Not started; blocked on decision C2 (spec §14.5). |

Six roadmap items conflict with hard constraints and are parked pending decisions — realtime presence, structured collections, synced blocks, offline/mobile-first, history storage, and the baseline's provenance. `BACKLOG.md` carries them under **Needs a decision**.

---

## 3. Known issues

**As of 2026-07-30: zero open issues.** Three audit passes ran back-to-back and every finding across all three is closed and merged to `main`:

- ~~**Full audit, 2026-07-29** — document coherence, spec-vs-code gaps, Obsidian compatibility.~~ **Closed.** 15 findings (2 critical, 2 high, 1 medium, 2 low), 9 issues filed and fixed: **#201** README stale (claimed PR5 when PR200+ was merged), **#202** `%%comment%%` leaked into rendered SPA/published output, **#203** `.excalidraw.md` files indexed as broken notes, **#204** front-matter `aliases:` not resolved for wikilinks, **#205** `^block-id`/`[[note#^block]]` block references unsupported, **#206** LaTeX/Mermaid unsupported (shipped as an opt-in, off-by-default hydration flag), **#207** dataview/Tasks-plugin degradation unverified (verified safe; surfaced #216 rather than being folded into the verification task), **#216** server-rendered HTML missing GFM task-list checkboxes, **#208** `BACKLOG.md` overloaded with six roles (split into `CHANGELOG.md`, `docs/decisions.md`, `docs/security-audit-2026.md`, `docs/visual-identity.md`). Report: `docs/20260729-jotter-audit.md`.
- ~~**Follow-up audit, 2026-07-29** — independently re-verified all 9 findings above were actually fixed in code, not just closed on GitHub.~~ **Closed.** All 9 confirmed fixed; no regressions. Surfaced 3 new low-priority gaps, all fixed: **#220** note rename/move didn't rewrite inbound `[[wikilinks]]` on disk in other notes, **#221** no "unlinked mentions" feature (notes mentioning a title/alias without an explicit link), **#222** no outgoing-links API/UI (only backlinks were exposed). Report: `docs/20260729-jotter-audit-followup.md`.
- ~~**Verification audit, 2026-07-30** — independently re-verified #220–#222 were actually fixed (including SPA wiring, not dead code) and re-checked the original 9 for regressions.~~ **Closed clean.** All fixed and SPA-wired; no regressions; no new findings. Report: `docs/20260730-jotter-audit-verification.md`.
- **UI audit vs. `docs/20260727-jotter-roadmap-ai-agent.md`, 2026-07-29: 14 gaps found and filed (#156–#169).** The backend has shipped far more than the SPA exposes — `BACKLOG.md`'s `[x]` marks track API delivery, not UI/UX delivery. 13 backend-complete features have zero frontend (version history, comments, notifications, typed properties, collections/table/board/calendar views, templates/daily notes, export, import, publish, audit log viewer, broken-link report, attachment browser, and search filters); nested folders render as a flat list rather than a tree (degraded, not missing). Prioritized and being worked one issue at a time:
  - ~~**#156 — search filters (title/tags/modified-date) not exposed.**~~ **Closed.** Filter bar added to `SearchResults.vue`, wired through to the existing backend `SearchCriteria` params. Verified in a real browser (not just unit tests): created two notes sharing a search term, confirmed the title filter narrows 2 matches to 1 and keeps the correct one. Merged via **PR #170**.
  - ~~**#169 — nested folders are a flat list, not a tree.**~~ **Closed.** Added a recursive `NoteTreeNode.vue`; `Sidebar.vue` now groups notes by folder segment into a collapsible tree (folders sorted alphabetically before files, default-expanded, note-count badge per folder). Leaf rows kept identical markup/classes to the old flat list, so `.notes-list` still contains each note's path text and `e2e/notes.spec.ts`'s existing assertion needed no changes. Verified via vitest (24/24), `vue-tsc`, the `a11y.spec.ts` axe check, and a manual Playwright run against the dev server with nested test notes. Merged via **PR #172**.
  - ~~**#168 — no attachment browser/delete UI.**~~ **Closed.** `getAttachments()`/`deleteAttachment()` were already defined in `api.ts` and fully supported server-side, but the SPA never called either — drag-drop upload in `NoteEditor.vue` was the only entry point. Added `AttachmentsPanel.vue` (workspace-level grid, thumbnails, delete-with-confirm), toggled from a new sidebar header button. Verified via vitest (29/29) and a manual Playwright run simulating a real drag-drop upload, listing, and delete. Merged via **PR #174**.
  - ~~**#157 — no version history/restore UI.**~~ **Closed.** `WorkspaceNoteRevisionController` (list/show/restore) and the deduplicated `note_revisions` table were fully wired server-side with no SPA UI for any of it. Added `HistoryPanel.vue` (revision list, read-only preview, restore action) opened via a new `history-btn` in `NoteEditor.vue`. Verified via vitest (36/36) and a manual Playwright run: saved two edits, previewed and restored the oldest of 3 revisions, confirmed the editor reflected it. Merged via **PR #176**.
  - ~~**#160 — no typed properties UI.**~~ **Closed.** Added `PropertiesPanel.vue` in `NoteEditor.vue` (list, typed add form, delete), wired to the existing property endpoints. Also found and fixed a real backend bug while verifying in a browser: `NoteProperty::type` wasn't cast to its enum, so `match($p->type)` in both controllers' `metadata()` always fell through to `default` — every non-string property value (numeric/boolean/datetime/list/json) silently returned `null` from the API despite being stored correctly. Fixed with a model-level enum cast; regression test confirmed to fail without it. Merged via **PR #178**.
  - ~~**#162 — no templates/daily-notes UI.**~~ **Closed.** New Note modal now offers an optional template picker (from `_templates/`) calling the existing `createFromTemplate` endpoint; a new daily-note header button gets-or-creates today's journal entry (idempotent). Verified via vitest (43/43) and a manual Playwright run confirming `{{title}}`/`{{workspace}}`/`{{date}}` substitution and no-duplicate-on-repeat-click. Merged via **PR #180**. This closes out Tier 2.
  - ~~**#158 — no comments UI.**~~ **Closed.** Added `CommentsPanel.vue` in `NoteEditor.vue` (list, add form, delete), wired to `WorkspaceCommentController`'s existing list/add/delete endpoints (with `@mention` parsing and the ownership-or-admin delete check hardened in #145). Verified via vitest (51/51) and a manual Playwright run: added two comments incl. one with an `@mention`, deleted one, confirmed the remainder. Merged via **PR #182**. **This completes Tier 2** (#156, #168, #157, #160, #162, #158 all shipped).
  - ~~**#166 — no audit log viewer UI.**~~ **Closed.** Added `AuditLogViewer.vue` (table + expandable JSON metadata) toggled from a sidebar header button. Also fixed a real backend bug found while verifying: `AuditLogQueryController` required an exact `tenant_id` match on top of `workspace_id`, but most `AuditRecorder::record()` call sites never populate `tenantId` — every `audit_log` row in the dev DB had a null `tenant_id`, so the endpoint returned zero results 100% of the time in practice. Fixed by scoping on `workspace_id` alone; strengthened `AuditLogQueryTest` to assert actual content instead of just JSON structure. Merged via **PR #184**.
  - ~~**#164 — no workspace import UI.**~~ **Closed.** Added an "Import Workspace Archive" modal wired to the existing `WorkspaceImportController` endpoint. Found and fixed two real bugs while verifying: (1) 5 sidebar icon buttons no longer fit the fixed 280px header, pushing `new-note-btn` outside the sidebar to silently swallow clicks — consolidated Attachments/Daily Note/Audit Log/Import into a "More actions" dropdown; (2) `storage/app/imports` missing from `entrypoint.sh`'s ownership loop (same bug class as #140), causing every import to fail with a permissions error on a fresh container. Merged via **PR #186**.
  - ~~**#163 — no workspace export UI.**~~ **Closed.** New "Export Workspace" item in the sidebar's More-actions menu, navigating to the existing `WorkspaceExportController` endpoint via `window.location.href` (no fetch/blob plumbing needed for a GET + `Content-Disposition: attachment` response). Verified via vitest and a manual Playwright run confirming a real zip download without leaving the SPA. Merged via **PR #188**.
  - ~~**#167 — no broken-link/orphan report UI.**~~ **Closed.** Added `LinkReportViewer.vue` ("Broken Links & Orphans" in the sidebar's More-actions menu), wired to the existing `WorkspaceLinkReportController` endpoint. Verified via vitest and a manual Playwright run confirming grouping/aggregation and click-to-navigate. Merged via **PR #190**.
  - ~~**#165 — no workspace publish UI.**~~ **Closed.** Added a "Publish Static Site" item to the sidebar's More-actions menu. Found and fixed two real backend bugs while verifying: `storage/app/public` missing from `entrypoint.sh`'s ownership loop (same class as #140/#164), and the endpoint's returned `site_url` pointing at an `index.html` that was never actually generated (the old test only passed by coincidence — its fixture note happened to be named `index.md`). Merged via **PR #192**. **This completes Tier 3** (#166, #164, #163, #167, #165 all shipped).
  - ~~**#159 — no notifications UI.**~~ **Closed.** Added a bell icon with unread-count badge and dropdown to the sidebar header, wired to the existing `WorkspaceNotificationController` endpoints. Found and fixed two bugs while verifying: an invalid `role="menu"`/`role="menuitem"` mismatch (changed to `role="region"`) and a dropdown that rendered off-screen (reused the wrong-edge `right: 0` positioning from the More-menu). Merged via **PR #194**.
  - ~~**#161** — Tier 4's last item (collections/table/board/calendar views, Milestone D).~~ **Closed** (table-view slice only). Added `CollectionsTableView.vue` (sortable/filterable table over `note_properties`, pagination), reachable from the sidebar's More-actions menu ("Table View"). Found and fixed two real bugs while verifying: `WorkspaceCollectionController`'s property-value filter only ever compared against `value_string`, so filtering on a numeric/boolean/datetime-typed property could never match (the existing test only exercised a string property, coincidentally masking it — same pattern as #160/#165); and the frontend sort coerced notes missing the sorted property to `0`, sorting them to the front instead of last. Board and calendar views were **not** part of this PR — per the issue's own sequencing ("table view first, board/calendar after it's stable"), those are tracked separately in **#197** ("UI: collections board/calendar views (Milestone D follow-up)"). Merged via **PR #196**.
  - ~~**#197** — collections board/calendar views (Milestone D follow-up to #161).~~ **Closed.** Added `CollectionsBoardView.vue` (kanban columns grouped by a chosen property, with a "No value" column) and `CollectionsCalendarView.vue` (agenda-style date buckets grouped by a chosen datetime property, with a "No date" bucket — a true month-grid calendar isn't feasible without a new endpoint, since the collections API only supports equality filters, not date ranges). Extracted the property-column/value-formatting logic shared across all three collection views into `services/collectionUtils.ts`. Found and fixed a real backend bug while verifying in a browser: Symfony's YAML parser resolves a bare (unquoted) ISO date scalar — the natural way to write a YAML date — to a Unix timestamp `int`, not a string, unless `Yaml::PARSE_DATETIME` is passed; `NotePropertyProjector` only checked `is_string()` for its datetime branch, so every unquoted date property was silently misclassified as `NUMERIC` instead of `DATETIME`. The existing `NotePropertyProjectionTest` only ever exercised a *quoted* date string, which coincidentally suppressed YAML's timestamp detection and masked this (same pattern as #160/#165/#161). Fixed by parsing front-matter with `Yaml::PARSE_DATETIME` and handling `DateTimeInterface` in the projector; regression test confirmed to fail without the fix. Merged via **PR #199**. **This closes out the original 14-issue UI-audit priority queue and its #197 follow-up — no open items remain from this audit.**
- ~~**#145 — `WorkspaceCommentController::destroy()` had no comment-ownership check.**~~ **Closed.** Any workspace member could delete any other member's comment — the endpoint only checked workspace-level authorization, not that the caller was the comment's author. Found via a codebase security audit, 2026-07-28. Fixed by requiring the caller be the comment's author or a workspace/tenant admin; regression tests added in `tests/Feature/WorkspaceCommentAuthorizationTest.php`. Merged via **PR #149**.
- ~~**#146 — MCP `read_note` tool calls undefined `VaultStorage::getNoteContent()`.**~~ **Closed.** Fatal error on every call; the actual method is `readContents()`. Found in the same audit. Confirmed both ways (revert reproduces 500, fix passes) via an extended `McpReadOnlyToolsTest`. Merged via **PR #150**.
- ~~**#147 — `GrandpaSSOnIdentityProvider` unconditionally creates every new SSO user as an admin.**~~ **Closed.** `is_admin => true` was hardcoded for new SSO-created users, and a null `is_admin` also resolved to admin — both now default to `false`. Gated behind `AUTH_PROVIDER=grandpasson` (not the default). Confirmed both ways (revert reproduces admin escalation, fix passes). Merged via **PR #151**. (The `WHERE expires_at > time()` behavior noted as "dead code" when this was filed was an artifact of that PR's own fabricated test schema, not GrandpaSSOn's real one — see #154, which found and fixed the actual bug: this code was never querying GrandpaSSOn's real tables at all.)
- ~~**#154 — `GrandpaSSOnIdentityProvider` queried Jotter's own tables, not GrandpaSSOn's.**~~ **Closed.** `DB::table('sessions')`/`DB::table('users')` go through Eloquent's default connection, which silently applies *this app's own* `DB_PREFIX` (e.g. `jt_` in production) — so this code queried Jotter's own local session table, never GrandpaSSOn's actual identity data, on shared hosting where the two apps share one MySQL database. A more fundamental bug than #147: even with that fix, real SSO sessions could never resolve. Fixed by querying GrandpaSSOn's real tables via the raw PDO connection with a separately configurable prefix (`jotter.sso.db_prefix` / `JOTTER_SSO_DB_PREFIX`, default `sso_`). Found via a cross-repo deployment-compatibility review for hub.taskconnect.com.br. Merged via **PR #153**.
- ~~**#148 — `main` has no branch protection requiring green CI before merge.**~~ **Closed.** This is exactly how #140 regressed silently after #49. Fixed via a repo-settings change (not a code diff): `main` now requires the `test` CI job to pass, `enforce_admins` is on, force-pushes/deletions are disabled. Verified live — a direct push was rejected by GitHub with `Required status check "test" is expected.`
- ~~**#140 — CI red on `main`.**~~ **Closed.** Root cause: `docker/php/entrypoint.sh` chowned `storage/app/private` for `www-data` but never `storage/app/vaults`, the default vault root (`config/jotter.php`). On a genuinely fresh checkout that directory doesn't exist, and `VaultPathGuard::ensureVaultRoot()`'s `mkdir()` failed with permission denied on the first note-create request — a real 500 that the old invisible `alert()` in `App.vue` hid from Playwright, making it look like a flaky timeout. Found by pulling the actual GitHub Actions logs, adding inline console/network diagnostics to `notes.spec.ts` and a Laravel-log dump to `.github/workflows/ci.yml`, then reproducing locally by resetting `storage/app` permissions to match a fresh checkout (confirmed failing without the fix, passing with it). `App.vue`'s `alert()` is also replaced with a DOM-visible `data-testid="error-banner"` regardless of trigger. Fixed and merged via **PR #144**, confirmed on two green GitHub Actions runs.
- ~~**`WorkspaceAuthorizationPlaceholder` is fully deleted from `app/`**~~ — **Closed (#142).** The six dead `withoutMiddleware(WorkspaceAuthorizationPlaceholder::class)` calls in `tests/Feature/WorkspaceNotesApiTest.php` are removed (not replaced with the real middleware class): every test in that file already authenticates as an `is_admin` user via `LocalIdentityProvider`, and `LocalIdentityProvider::isAuthorizedForWorkspace()` short-circuits to `true` for admins regardless of membership, so no bypass was ever needed. Verified via `jt test` (98 PHPUnit, 20 Vitest, all pass) and grepped the rest of the suite for stray references to the deleted class — none found. Merged via **PR #144**.
- **The WebDAV adapter is hand-rolled, not SabreDAV.** `sabre/dav` is not a dependency despite the commit message and earlier status notes saying so.
- **Editor chrome / Notion-parity audit, 2026-08-03: 14 gaps found and filed (#250–#263).** `docs/20260803-jotter-editor-chrome-notion-parity-audit.md` — triggered by a production report that the metadata area takes too much space even when collapsed, and "it still doesn't feel like Notion." Prioritized and being worked one issue at a time (same pattern as the 2026-07-29 UI audit above):
  - ~~**#250 — empty metadata panels always mount, costing ~335px on a note with no data.**~~ **Closed.** Backlinks, Outgoing Links, and Unlinked Mentions panels (purely derived/read-only, no creation UI) are now hidden entirely via `v-if` when their list is empty. Properties and Comments panels are deliberately left mounted even when empty — both have a persistent "add" form that is the only entry point to create a note's first property/comment, so hiding them would remove that affordance rather than just tidy the layout. This is a scoped narrowing of the issue's literal "all five panels" framing, not an incomplete fix. Merged via **PR #264**.
  - ~~**#251 — collapsed panel shell still costs ~67px each (140px of it pure dead space).**~~ **Closed.** `PanelHeader.vue`'s `margin-bottom` and each panel's `padding-bottom` — both sized for a body that isn't rendered when collapsed — are now zeroed via a `panel-header-collapsed`/`panel-collapsed` class applied to each root element in the collapsed state. Top/left/right padding preserved so the header inset is unaffected. Applied identically across all 5 panels. Merged via **PR #266**.
  - ~~**#252 — five stacked metadata sections steal fixed height instead of scrolling away.**~~ **Closed** (scoped CSS fix, not the full D.1 rework). `.editor-container` changed from `overflow: hidden` to `overflow-y: auto` so the whole column (canvas + panels) scrolls as one unit instead of clipping; `.editor-body` gets a `min-height: 50vh` safety floor (not the primary sizing mechanism — the container's own ancestor chain is `height: 100%` up to a `100vh` root, so `flex: 1` already had a real pixel budget; the floor just stops tall panel stacks from squeezing it under a usable minimum). Verified via a real Playwright browser run (`jt.sh e2e`), not just jsdom unit tests, given the flexbox+overflow risk on the core editing surface — isolated via `git stash` that 2 of 4 e2e tests fail identically with and without this change (a pre-existing flake, not a regression). Deliberately NOT the full inline-properties/consolidated-Links/comments-drawer rework that issues #256/#258/#262 cover — this is an interim layout fix, revisit the `min-height` constant once those land. Merged via **PR #267**.
  - ~~**#253 — permanent "Add cover" button costs fixed chrome height even when the note has no cover.**~~ **Closed.** The button is now wrapped in a hover/focus-reveal zone (`.editor-title-zone`), matching how the existing cover Change/Remove actions already reveal on hover over a set cover image. Collapses to zero footprint via `max-height: 0; padding: 0 var(--space-2); opacity: 0; border-bottom: 0 solid transparent`, expanding on `:hover`/`:focus-within` of the title zone. A review-caught bug (`border-bottom: 1px solid transparent` still consumed 1px of layout height even "invisible," since `max-height`/`overflow:hidden` clip content height but not the border box) was fixed by animating `border-width` alongside opacity/max-height/padding. Deliberately keeps the push-down reveal (reflow) rather than converting to an absolute-positioned overlay like the existing cover-actions fade — there's no existing reserved space to overlay onto when no cover is set, unlike the cover-actions case. Merged via **PR #268**.
  - ~~**#254 — explicit Save button + permanent stats footer instead of autosave.**~~ **Closed.** Content now autosaves 1s after typing stops (debounced `setTimeout`, re-armed each keystroke); a transient status pill in the header shows "Saving…" / "Unsaved changes" / "Saved" (auto-dismissing 2s after a successful save) in place of the old explicit Save button. Manual Ctrl+S/Cmd+S still works and flushes any pending autosave timer immediately, avoiding a duplicate save. The permanent stats footer (word/char count, reading time) is removed entirely, replaced with an on-demand popover toggled by a button next to History. All timers (autosave, saved-indicator auto-dismiss) are cleared on unmount and on note-switch to prevent a stale timer firing against the wrong note — the note-switch case was itself a review-caught gap (only the autosave timer was being cleared, not the saved-indicator timer) fixed before merge. 8 new Vitest tests cover debounce timing, manual-save flush, indicator lifecycle, stats-popover toggle, and the note-switch timer-clearing edge cases. Merged via **PR #269**.
  - ~~**#256 — slash menu is fully built and tested but never mounted anywhere.**~~ **Closed.** Wired a `/` trigger into `NoteEditor.vue`'s `handleInput`, mirroring the existing `[[` wikilink trigger's structure: a `/` at the start of a line or right after a space opens `SlashMenu.vue` with a live filter query; selecting a block (click or via the component's own arrow/Enter/Escape keyboard handling, delegated from `handleKeyDown`) inserts that block's `syntax` string from `blockRegistry.ts` at the trigger position. `SlashMenu.vue` and `blockRegistry.ts` needed no changes — the "hard part" the issue described was already written and tested, only the wiring was missing. 8 new Vitest tests cover trigger-at-line-start, no-trigger mid-word, trigger after space/newline, block insertion + menu close, Escape-to-close, query filtering, wikilink-trigger closing the slash menu, and multi-line block insertion with correct cursor placement. `STATUS.md`'s pre-existing "Slash-Command Insertion Menu" bullet (§2) is corrected to stop implying this was already reachable by users.
  - ~~**#257 — note title is read-only chrome instead of editable page content.**~~ **Closed.** The page icon + title moved out of `.editor-bar` (which shared a row with 6 toolbar controls and carried a `border-bottom`) into their own block in the scrolling canvas, below the cover and above the body — `.editor-bar` is now a thin breadcrumb-only strip. The title is a real auto-growing `<textarea>` persisted to `frontmatter.title` on a debounce (same pattern as the body's own autosave), not spliced into the body's `#` heading as the issue's proposed fix literally suggested — writing into the heading would make the title render twice (once in the new field, once as the still-present `# heading` line inside the body textarea/preview), a duplication bug worse than the one being fixed. `frontmatter.title` already has the highest precedence in the server's title resolution (`MarkdownDocument::resolveTitle`), so this is a smaller, more correct change: one line added to `NotePropertyProjector`'s exclusion list (alongside `icon`/`cover`, so it doesn't leak into the generic Properties panel) plus a `PHPUnit` test, and pure frontend wiring reusing the exact same read/write pattern the icon field already uses. Enter moves focus into the body instead of inserting a newline. 5 new Vitest tests plus the e2e `editor-title` assertion updated from `toContainText` to `toHaveValue` (it's a form field now, not static text).
  - ~~**#258 — Properties panel is an add-form; existing values cannot be edited in place.**~~ **Closed.** Each row's value is now the editing surface itself (click it to edit), matching its existing type — text input for string/list, number input for numeric, `datetime-local` for datetime, an immediate-commit checkbox for boolean — instead of requiring delete-and-recreate. Reuses the exact same `add-property` emit/`setNoteProperty` API the Add form already calls (the backend already upserts by name, no new endpoint needed). The per-row type badge is dropped per the issue's ask — type is chosen once at creation via the Add form's `<select>` and never re-stated in the row. `json`-typed properties (only reachable via direct frontmatter edits, never via this panel) are left read-only — arbitrary nested structure has no safe single-input edit surface. Commit-on-blur has the same re-entrant-call guard as `NoteEditor.vue`'s `confirmEditingIcon`. 8 new Vitest tests cover pre-filled edit state, commit-on-Enter, cancel-on-Escape, numeric/list coercion, immediate-commit boolean, and the json-property exclusion. Deliberately NOT the A.3/A.4 rework (inline panels in the page scroll flow) the issue notes pairs naturally with this — that's a separate, larger structural change.
  - ~~**#259 — sidebar is fixed 280px with no collapse, hover-peek, or drag-resize.**~~ **Closed** (collapse only, per the issue's own scoping — drag-resize called out as a nice-to-have, not required). A "Collapse Sidebar" item in `Sidebar.vue`'s existing "More actions" overflow menu collapses it to zero width (`width/min-width: 0; overflow: hidden`, desktop widths only — mobile's existing transform-based drawer is untouched), handing the freed width to `.main-content` automatically via the existing `flex: 1` — no `App.vue` changes needed. Persisted via the same `useCollapsiblePanel` `localStorage` pattern already used by the metadata panels (`sidebar-desktop` key). A floating `position: fixed` expand button (self-contained in `Sidebar.vue`, escapes the collapsed ancestor's `overflow: hidden` since fixed positioning is viewport-relative) reappears in the top-left corner while collapsed. A real e2e regression was caught and fixed before merge: an initial always-visible icon-button placement (rather than the overflow-menu item shipped) pushed the already-tight header icon row past the sidebar's fixed width, spilling into `.main-content` and intercepting clicks on the editor underneath — root-caused via `git checkout <parent-commit> -- Sidebar.vue` isolation (3/3 fails with the change, 2/2 passes without) before landing the overflow-menu placement instead. 4 new Vitest tests cover expanded-by-default, collapse, re-expand, and cross-remount persistence.
  - ~~**#260 — bordered-card list rows and ALL-CAPS section headers contradict the token layer.**~~ **Closed.** Every list-item row across `PropertiesPanel.vue`, `BacklinksPanel.vue`, `OutgoingLinksPanel.vue`, `UnlinkedMentionsPanel.vue`, and `CommentsPanel.vue` (`.property-item`, `.backlink-item`, `.outgoing-link-item`, `.mention-item`, `.comment-item`) had its own `background`/`border`/`border-radius` — a bordered card per row, contradicting `docs/visual-identity.md` §1's own stated principle (border/tint at the container level, not per row). The border/background is now removed from each row; the three clickable rows (`.backlink-item`, `.outgoing-link-item`, `.property-item`) get a plain `background: var(--color-hover)` on `:hover` in its place, while `.mention-item` (its main area emits `select-note` on click) keeps the same treatment — `.comment-item`, which has no click handler at all (only a separate delete button), deliberately gets NO hover tint, since a hover affordance on a non-interactive row would be misleading. Each panel's own container `border-top` (already present on all five) is the only border that remains, matching Notion's borderless, hover-tinted list rows. `PanelHeader.vue`'s `text-transform: uppercase; letter-spacing: 0.05em` is dropped for sentence-case section labels, font-size left at the existing 0.75rem — an initial companion font-size bump was reverted after review flagged it as unrequested scope beyond the issue's literal case-only ask. Block-level hover affordances (drag handle, `+` insert) are explicitly out of scope per the issue's own text — noted there for future reference only. CSS-only; no new tests (nothing jsdom can meaningfully assert about here), verified via the existing per-panel Vitest suites (260 tests, all pass unaffected).
  - ~~**#261 — comments cannot be anchored to a text selection from the UI.**~~ **Closed.** The backend already supported anchoring (`addNoteComment`'s `anchorLine` param, `comment.anchor_line` already rendered in `CommentsPanel.vue`) — there was just no UI entry point besides the global form at the bottom of the page. Releasing a mouse selection in `.markdown-textarea` now shows a small floating "💬 Comment" button near the release point; clicking it opens a small inline composer (textarea + Cancel/Comment), and submitting computes the anchor line from the selection's start index (`content.slice(0, selectionStart).split('\n').length`) and reuses the exact same `handleAddComment`/`addNoteComment` call path the global form already used, just with the anchor now populated. Positioned from the `mouseup` event's own `clientX`/`clientY` rather than a caret-position mirror-div, since a plain `<textarea>` (not contenteditable) has no per-character DOM Range API to measure exact caret pixel position from — this is deliberately mouse-selection-driven only, not keyboard (shift+arrow) selection, matching how Google Docs/Notion-style selection-comment affordances are typically triggered. The trigger button hides on typing (`handleInput`) and on note-switch. 6 new Vitest tests cover trigger show/hide on selection, composer open/close, Escape-cancels-without-submitting, and the anchor-line computation.
  - ~~**#262 — no right-hand drawer in the app shell; secondary views replace the page instead of overlaying it.**~~ **Closed** (drawer mechanism only, per the issue's own scoping recommendation — migrating Graph/Attachments/Audit Log/etc. into it is left for future issues). `App.vue` gains a single `<div id="app-right-drawer">` Teleport mount point, a sibling of `.main-content`. `NoteEditor.vue`'s previously always-inline `CommentsPanel` (stacked below the editor body) now teleports into that mount point as a `position: fixed` slide-over (`width: min(360px, 100vw)`, full height, `z-index: 40` — above the mobile sidebar backdrop's 30), toggled by a new comment-count badge button in the editor toolbar. Deliberately an overlay, not a layout-pushing panel, so it can be open at the same time as the note editor rather than unmounting or resizing it — verified via a real Playwright e2e test asserting both the drawer and the markdown textarea are visible simultaneously. Comments state/logic stay owned by `NoteEditor.vue` (only the DOM mount point moved); the drawer's open/closed state deliberately does NOT reset on note-switch, matching persistent sidebar-like behavior rather than per-document ephemeral UI state. 4 new Vitest tests plus the e2e drawer test; an existing `NoteEditor.spec.ts` test asserting Comments always renders inline was split since that's no longer true — Properties keeps the old always-mounted behavior, Comments now only mounts when the drawer is open.
  - **#263 — Markdown textarea + Edit/Split/Preview toggle vs. real Notion-feel — NOT implemented, by design.** P3 and explicitly "decision needed," not an implementation task (the issue's own text: this collides with the Markdown-on-disk invariant and must be decided deliberately, not drifted into via smaller PRs). Recorded as a pending decision in `BACKLOG.md`'s "Needs a decision" section instead of shipping code; the audit's #250–#262 all proceed independently of this decision. This closes out the 2026-08-03 editor-chrome/Notion-parity audit's issue queue (#250–#263).
  - **#263 decided 2026-08-05: option (b), inline WYSIWYG over Markdown via Milkdown.** Full rationale in `docs/decisions.md`, phased breakdown in `docs/20260805-jotter-wysiwyg-editor-epic-spec.md`. Five sequenced issues filed (#321–#325, WY.1–WY.5), each gated on the previous PR merged and green — no implementation has landed yet, this is the decision + epic-scoping PR only, matching how #278/#309 recorded decisions and filed issues without shipping code in the same PR.
- ~~**No workspace-creation UI anywhere in the app.**~~ **Fixed.** `AdminPanel.vue` — a fully-built component with a working "Create Workspace" form wired to `POST /api/admin/workspaces` — was never imported or mounted anywhere (only its own spec file referenced it, the same class of bug as #256's orphaned slash menu). Two more real bugs surfaced once it was found: (1) every one of its API calls used raw `fetch()` instead of the app's shared axios instance, which has automatic Sanctum XSRF-cookie-to-header handling — raw `fetch()` doesn't set that header, so every mutating admin call would 419 in production; (2) the create-workspace form was missing the `tenant_id` field the backend requires (`required|integer|exists:tenants,id`), so submission would always 422 even with CSRF fixed. Fixed all three: exported the shared `api` axios instance from `services/api.ts` and migrated every AdminPanel call onto it; added a tenant `<select>` (auto-selected when only one tenant exists) plus a client-side guard blocking submission without one; added an "Admin Panel" item to `Sidebar.vue`'s "More actions" menu gated by `currentUser?.is_admin` (server-side `authorizeAdmin()` remains the actual enforcement — this is UX only), and mounted `<AdminPanel>` as a modal overlay in `App.vue`, refreshing the workspace switcher's list on close so a newly created workspace appears without a reload. Verified end-to-end via a real Playwright test (login as admin, open panel, create a workspace, confirm it appears in both the panel and the sidebar's workspace switcher) plus new/updated Vitest coverage for the axios migration, tenant auto-select, and the `is_admin` menu gating.
- ~~**#280 — password management (self-service change + admin reset) has no UI.**~~ **Closed.** Same audit that found the orphaned AdminPanel surfaced two more backend endpoints with zero frontend callers: `POST /api/auth/change-password` (self-service, already listed as delivered in this file's §1) and `POST /api/admin/users/{user}/reset-password`. Added a `ChangePasswordModal.vue` (styled like `CoverImageModal.vue`) reachable from a new icon button in the sidebar's user-footer, gated to `authProvider === 'local'` (SSO users authenticate elsewhere) — auto-closes ~1.5s after a successful change. Added a "Reset Password" button per row in AdminPanel's Users tab using `window.prompt()` for the new value, matching the existing `archiveWorkspace` confirm-dialog pattern. A review-caught gap: `AdminUserController::changePassword()` was missing the `ensureLocalProvider()` guard its sibling admin-user endpoints all have — added, with a new PHPUnit assertion. Verified via 2 new Playwright e2e tests (self-service change, admin reset via the browser's native prompt dialog) plus new/updated Vitest coverage across `ChangePasswordModal`, `Sidebar`, `AdminPanel`, and `App`.
- ~~**#282 — admin workspace edit and property-name autocomplete have no UI.**~~ **Closed.** Same route-vs-frontend-caller audit found two more: `PUT /api/admin/workspaces/{workspace}` (rename/edit) and `getWorkspaceProperties()` (distinct name/type pairs already used anywhere in the workspace, meant to drive Add-property autocomplete). Added inline Edit per row in AdminPanel's Workspaces tab — name and slug only; `vault_path` is deliberately excluded, since `GET /workspaces` (the endpoint AdminPanel's own list is populated from) never returns it, and sending an empty string would trip `VaultRootGuard::validate('')` and corrupt the real path. Wired `getWorkspaceProperties()` into `PropertiesPanel.vue`'s Add-property name field as a `<datalist>`, auto-adopting a known property's existing type when its name is selected — refetched on mount AND on focus of the field, since `NoteEditor.vue` (and its `PropertiesPanel`) is reused across note switches rather than remounted, so a property added on one note wouldn't otherwise appear when adding one on another until a full reload. Verified via 2 new Playwright e2e tests (edit a throwaway workspace's name, cross-note autocomplete) plus new/updated Vitest coverage across `AdminPanel` and `PropertiesPanel`.
- ~~**Mobile-browser responsiveness audit, 2026-08-03: 1 blocker, 3 major, 3 minor findings.**~~ **Closed** (all real findings fixed; 2 findings turned out inaccurate on verification and were skipped — see below). Blocker: `Sidebar.vue`'s note delete button was hover-only (`opacity: 0` revealed on `:hover`), unreachable on touch devices — fixed with the same `@media (max-width: 768px)` opacity/44px-touch-target pattern `NoteTreeNode.vue` already used for its own delete button. Majors: `Sidebar.vue`'s and `CoverImageModal.vue`'s shared `.modal-card` had a fixed `width: 360px` with no viewport cap, taking ~96% of an iPhone SE's width — both given `max-width: 90vw`; `AdminPanel.vue`'s modal had no scroll/wrap handling for small screens — `.admin-modal-container` gained `max-height: 90vh; overflow-y: auto`, `.admin-tabs` scrolls horizontally instead of squashing, `.admin-list-item` wraps instead of overflowing. Minor: `Sidebar.vue`'s version text was 11px (below the ~12px readable floor), bumped to `0.75rem`. Two findings from the initial audit did not survive verification and were left as-is: a claimed `769px`/`768px` breakpoint inconsistency in `Sidebar.vue` (no `769px` exists anywhere in the codebase — false positive) and the sidebar's `width: min(85vw, 320px)` mobile drawer sizing (a correct fluid-clamp pattern, not a bug). Verified via `vue-tsc -b && vite build` after each round of fixes. Merged via **PR #284** (bundled with #254's autosave work, since both were on the same long-lived branch).
- ~~**`BACKLOG.md` was self-contradictory**~~ — the "Recorded Decisions" and "Needs a decision" sections disagreed about whether C1–C6 were resolved. Fixed; see #141.

---

## 4. Next

1. ~~Merge the `claude/fix-140-142-ci-and-dead-tests` PR~~ — **done.** PR #144 merged 2026-07-28, `main` is green on two consecutive GitHub Actions runs, #140 and #142 closed.
2. ~~Clean up **#142**~~ — done in the same PR.
3. ~~**Security/correctness audit findings #145–#148**~~ — **all closed.** #145, #146, #147 fixed and merged (PR #149, #150, #151); #148 fixed via a `main` branch-protection rule (required status check, `enforce_admins`, no force-push/delete), verified live. From this point on, every change to `main` (including doc-only updates like this one) must go through a PR with a green `test` check — direct pushes are rejected.
4. Resolve the remaining §14.5 decision — roadmap baseline provenance is the only one still open; C1–C6 are recorded resolved (see `docs/decisions.md`).
5. Milestone A/B/C/D are all recorded complete, and CI is green on `main` under branch protection (#148, closed — required status check, `enforce_admins`, no force-push/delete), so this is trustworthy for the current HEAD, not just a point-in-time claim.
6. **Visual identity (#96)** — cross-cutting presentation workstream adopting a shared dark/purple design system with semantic tokens, Open Sans, and WCAG 2.2 AA across the SPA, the Laravel shell, and the published static site. Recorded complete; now unblocked since item 1 is done.
7. ~~**Three-pass audit cycle (2026-07-29 → 2026-07-30)**~~ — **done.** Full audit → fix all 9 findings → independent re-verification → fix the 3 findings it surfaced → independent re-verification again. Zero open issues as of 2026-07-30 (see §3). The only remaining open item in this whole file is #4 above (roadmap baseline provenance), which is a decision, not a defect.
8. **WYSIWYG editor epic (decided 2026-08-05, resolves #263).** `docs/20260805-jotter-wysiwyg-editor-epic-spec.md`. ~~**WY.1 (#321)** — Milkdown dependency + Markdown round-trip fidelity harness, no UI change.~~ **Done.** `@milkdown/kit` (commonmark + gfm presets) added; `frontend/src/services/__tests__/wysiwygRoundTrip.spec.ts` covers every syntax feature `markdown.ts`/`MarkdownServerRenderer.php` special-cases. 11 fixtures round-trip losslessly (headings, emphasis, links/images, nested ordered lists, code fences incl. `mermaid`/dataview-as-opaque, standalone `^block-id` refs). 8 fixtures documented as known gaps, **required scope for WY.3 (#323)**: front matter is destroyed entirely (CommonMark has no concept of it — WY.2/WY.3 must strip/reattach it outside Milkdown's parse); `[[wikilink]]`, `![[embed]]`, and `> [!NOTE]` callout brackets get backslash-escaped (no native node exists yet); unordered/task-list bullet markers and table separator rows are cosmetically renormalized (semantically lossless, but not byte-identical). ~~**WY.2 (#322)** — `NoteEditorWysiwyg.vue`, opt-in `'live'` viewMode, standard CommonMark+GFM nodes only.~~ **Done.** New `NoteEditorWysiwyg.vue` wraps a headless `createWysiwygEditor()` (Milkdown commonmark+gfm+listener) behind the same `content`/`update:content` v-model contract `editableContent`'s textarea binding already uses, so the existing debounced autosave and Ctrl+S flush (#254) work unchanged. `viewMode` gains a fourth `'live'` option (additive, opt-in, mounted via `v-if`). Front matter is stripped before Milkdown ever sees it and reattached unchanged on every emit (`frontMatterGuard.ts`), closing WY.1's most severe documented gap for this mode. `[[wikilink]]`/`![[embed]]`/`> [!NOTE]` callouts still pass through as plain text (native nodes are WY.3/#323 scope). ~~**WY.3 (#323)** — native nodes for wikilinks/embeds/callouts/toggles/tables + slash menu on the WYSIWYG surface.~~ **Done.** `frontend/src/services/wysiwygNodes/{wikilink,embed,callout,toggle}.ts` add dedicated ProseMirror nodes (each with its own `$remark` parse/stringify pair) for `[[wikilink]]`, `![[embed]]`, `> [!NOTE]` callouts, and single-line `<details>` toggles — all four moved from WY.1's KNOWN_GAP_FIXTURES to LOSSLESS_FIXTURES in the round-trip harness. GFM tables were already native via the `gfm` preset (WY.1), no new node needed. `wysiwygEditor.ts` gained `insertMarkdown`/`insertBlockReplacingSlashQuery` plus an `onSlashQuery` trigger-detection hook; `SlashMenu.vue` (#256) now works in Live mode against the same `blockRegistry.ts` definitions, not a fork. Known limit, documented not silently dropped: toggle bodies spanning a blank line stay inert raw HTML (only the single-line form blockRegistry.ts's own snippet uses is a native node). ~~**WY.4 (#324)** — port comment-anchoring and history/restore off textarea coordinate hacks.~~ **Done.** `wysiwygEditor.ts`'s `getSelectionAnchorLine()` computes the comment anchor from an exact ProseMirror document-structure mapping (serialize the doc's preceding sibling blocks, count newlines) instead of the textarea's `selectionStart` character-offset heuristic; the comment-trigger button/composer moved out of `.textarea-wrapper` to a sibling of both editor surfaces (same reason `SlashMenu` moved in WY.3) so they render in Live mode at all. Verifying history/restore against Live surfaced a real, pre-existing bug (not WY.4-introduced, but blocking its own acceptance criterion): `handleRestoreRevision` keeps the same `note.id`, which the autosave-loop guard (`if (newNote.id === currentNoteId) return`) was silently swallowing — restoring a revision appeared to succeed but the editor kept showing the pre-restore text, on *any* surface, not just Live. Fixed by resetting `currentNoteId` before the restore's `select-note` emit. ~~**WY.5 (#325)** — make "Live" the default view mode; keep raw source as an opt-in fallback.~~ **Done — epic complete (WY.1-WY.5), resolves #263.** `viewMode` defaults to `'live'`; `Edit`/`Split`/`Preview` stay fully reachable via the toggle, not removed (retiring the toggle entirely is deferred, per the epic spec §6 — this PR explicitly decided to keep `Preview`, since retiring any toggle option now would cross into that same deferred non-goal, and `MarkdownPreview.vue`'s rendering is reused outside the toggle regardless). Opening any existing note now lands in a WYSIWYG surface with no Markdown syntax visible; switching to `Edit` still shows the exact underlying source unchanged. **Post-epic follow-up (no issue number, direct request):** a Notion-style floating formatting toolbar (Bold/Italic/Strike/Code) now appears above a text selection on the Live surface, wired to Milkdown's own `toggleStrongCommand`/`toggleEmphasisCommand`/`toggleStrikethroughCommand`/`toggleInlineCodeCommand` — no new mark schema, just a UI on top of already-present commands.
