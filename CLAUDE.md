# Repository instructions

Read `AGENTS.md` and `docs/jotter-initial-spec-and-build-plan.md` before changing code.

Use only the `jt` Docker wrappers for runtime, dependency, database, build, and test commands. Keep changes inside the active PR unit, preserve the release secret checks, and update `STATUS.md` and `BACKLOG.md` when scope changes.
